/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLAIM_LEVEL_CERTIFICATE_DISCRIMINATOR = new Uint8Array([
  71, 62, 234, 114, 76, 122, 16, 127,
]);

export function getClaimLevelCertificateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_LEVEL_CERTIFICATE_DISCRIMINATOR,
  );
}

export type ClaimLevelCertificateInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountCertificate extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? ReadonlyAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountCertificate extends string
        ? WritableAccount<TAccountCertificate>
        : TAccountCertificate,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimLevelCertificateInstructionData = {
  discriminator: ReadonlyUint8Array;
  level: number;
};

export type ClaimLevelCertificateInstructionDataArgs = { level: number };

export function getClaimLevelCertificateInstructionDataEncoder(): FixedSizeEncoder<ClaimLevelCertificateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["level", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CLAIM_LEVEL_CERTIFICATE_DISCRIMINATOR,
    }),
  );
}

export function getClaimLevelCertificateInstructionDataDecoder(): FixedSizeDecoder<ClaimLevelCertificateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["level", getU8Decoder()],
  ]);
}

export function getClaimLevelCertificateInstructionDataCodec(): FixedSizeCodec<
  ClaimLevelCertificateInstructionDataArgs,
  ClaimLevelCertificateInstructionData
> {
  return combineCodec(
    getClaimLevelCertificateInstructionDataEncoder(),
    getClaimLevelCertificateInstructionDataDecoder(),
  );
}

export type ClaimLevelCertificateAsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountCertificate extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  certificate: Address<TAccountCertificate>;
  systemProgram?: Address<TAccountSystemProgram>;
  level: ClaimLevelCertificateInstructionDataArgs["level"];
};

export async function getClaimLevelCertificateInstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountCertificate extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ClaimLevelCertificateAsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountCertificate,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimLevelCertificateInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountCertificate,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: false },
    certificate: { value: input.certificate ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.certificate),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimLevelCertificateInstructionDataEncoder().encode(
      args as ClaimLevelCertificateInstructionDataArgs,
    ),
    programAddress,
  } as ClaimLevelCertificateInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountCertificate,
    TAccountSystemProgram
  >);
}

export type ClaimLevelCertificateInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountCertificate extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  certificate: Address<TAccountCertificate>;
  systemProgram?: Address<TAccountSystemProgram>;
  level: ClaimLevelCertificateInstructionDataArgs["level"];
};

export function getClaimLevelCertificateInstruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountCertificate extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ClaimLevelCertificateInput<
    TAccountUser,
    TAccountUserStats,
    TAccountCertificate,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimLevelCertificateInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountCertificate,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: false },
    certificate: { value: input.certificate ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.certificate),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimLevelCertificateInstructionDataEncoder().encode(
      args as ClaimLevelCertificateInstructionDataArgs,
    ),
    programAddress,
  } as ClaimLevelCertificateInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountCertificate,
    TAccountSystemProgram
  >);
}

export type ParsedClaimLevelCertificateInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    certificate: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClaimLevelCertificateInstructionData;
};

export function parseClaimLevelCertificateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimLevelCertificateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      certificate: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimLevelCertificateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
