/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel0StatePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const VERIFY_AND_CLOSE_LEVEL0_DISCRIMINATOR = new Uint8Array([
  88, 24, 131, 11, 54, 23, 3, 18,
]);

export function getVerifyAndCloseLevel0DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_AND_CLOSE_LEVEL0_DISCRIMINATOR,
  );
}

export type VerifyAndCloseLevel0Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel0State extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel0State extends string
        ? WritableAccount<TAccountLevel0State>
        : TAccountLevel0State,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyAndCloseLevel0InstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VerifyAndCloseLevel0InstructionDataArgs = {};

export function getVerifyAndCloseLevel0InstructionDataEncoder(): FixedSizeEncoder<VerifyAndCloseLevel0InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VERIFY_AND_CLOSE_LEVEL0_DISCRIMINATOR,
    }),
  );
}

export function getVerifyAndCloseLevel0InstructionDataDecoder(): FixedSizeDecoder<VerifyAndCloseLevel0InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVerifyAndCloseLevel0InstructionDataCodec(): FixedSizeCodec<
  VerifyAndCloseLevel0InstructionDataArgs,
  VerifyAndCloseLevel0InstructionData
> {
  return combineCodec(
    getVerifyAndCloseLevel0InstructionDataEncoder(),
    getVerifyAndCloseLevel0InstructionDataDecoder(),
  );
}

export type VerifyAndCloseLevel0AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel0State extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level0State?: Address<TAccountLevel0State>;
};

export async function getVerifyAndCloseLevel0InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel0State extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel0AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyAndCloseLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level0State: { value: input.level0State ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level0State.value) {
    accounts.level0State.value = await findLevel0StatePda({
      user: expectAddress(accounts.user.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level0State),
    ],
    data: getVerifyAndCloseLevel0InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State
  >);
}

export type VerifyAndCloseLevel0Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel0State extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level0State: Address<TAccountLevel0State>;
};

export function getVerifyAndCloseLevel0Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel0State extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel0Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State
  >,
  config?: { programAddress?: TProgramAddress },
): VerifyAndCloseLevel0Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel0State
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level0State: { value: input.level0State ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level0State),
    ],
    data: getVerifyAndCloseLevel0InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State
  >);
}

export type ParsedVerifyAndCloseLevel0Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level0State: TAccountMetas[2];
  };
  data: VerifyAndCloseLevel0InstructionData;
};

export function parseVerifyAndCloseLevel0Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyAndCloseLevel0Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level0State: getNextAccount(),
    },
    data: getVerifyAndCloseLevel0InstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
