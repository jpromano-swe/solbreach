/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel1StatePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INIT_LEVEL1_DISCRIMINATOR = new Uint8Array([
  250, 112, 168, 131, 246, 44, 13, 80,
]);

export function getInitLevel1DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_LEVEL1_DISCRIMINATOR);
}

export type InitLevel1Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel1State extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel1State extends string
        ? WritableAccount<TAccountLevel1State>
        : TAccountLevel1State,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLevel1InstructionData = { discriminator: ReadonlyUint8Array };

export type InitLevel1InstructionDataArgs = {};

export function getInitLevel1InstructionDataEncoder(): FixedSizeEncoder<InitLevel1InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INIT_LEVEL1_DISCRIMINATOR }),
  );
}

export function getInitLevel1InstructionDataDecoder(): FixedSizeDecoder<InitLevel1InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitLevel1InstructionDataCodec(): FixedSizeCodec<
  InitLevel1InstructionDataArgs,
  InitLevel1InstructionData
> {
  return combineCodec(
    getInitLevel1InstructionDataEncoder(),
    getInitLevel1InstructionDataDecoder(),
  );
}

export type InitLevel1AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel1State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level1State?: Address<TAccountLevel1State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitLevel1InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel1State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel1AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level1State: { value: input.level1State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level1State.value) {
    accounts.level1State.value = await findLevel1StatePda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level1State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel1InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State,
    TAccountSystemProgram
  >);
}

export type InitLevel1Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel1State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level1State: Address<TAccountLevel1State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitLevel1Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel1State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel1Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLevel1Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel1State,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level1State: { value: input.level1State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level1State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel1InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State,
    TAccountSystemProgram
  >);
}

export type ParsedInitLevel1Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level1State: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitLevel1InstructionData;
};

export function parseInitLevel1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLevel1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level1State: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLevel1InstructionDataDecoder().decode(instruction.data),
  };
}
