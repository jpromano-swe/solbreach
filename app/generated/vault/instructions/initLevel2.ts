/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel2StatePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INIT_LEVEL2_DISCRIMINATOR = new Uint8Array([
  176, 62, 169, 146, 12, 173, 203, 149,
]);

export function getInitLevel2DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_LEVEL2_DISCRIMINATOR);
}

export type InitLevel2Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel2State extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel2State extends string
        ? WritableAccount<TAccountLevel2State>
        : TAccountLevel2State,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLevel2InstructionData = { discriminator: ReadonlyUint8Array };

export type InitLevel2InstructionDataArgs = {};

export function getInitLevel2InstructionDataEncoder(): FixedSizeEncoder<InitLevel2InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INIT_LEVEL2_DISCRIMINATOR }),
  );
}

export function getInitLevel2InstructionDataDecoder(): FixedSizeDecoder<InitLevel2InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitLevel2InstructionDataCodec(): FixedSizeCodec<
  InitLevel2InstructionDataArgs,
  InitLevel2InstructionData
> {
  return combineCodec(
    getInitLevel2InstructionDataEncoder(),
    getInitLevel2InstructionDataDecoder(),
  );
}

export type InitLevel2AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel2State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level2State?: Address<TAccountLevel2State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitLevel2InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel2State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel2AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level2State: { value: input.level2State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level2State.value) {
    accounts.level2State.value = await findLevel2StatePda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level2State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel2InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountSystemProgram
  >);
}

export type InitLevel2Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel2State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level2State: Address<TAccountLevel2State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitLevel2Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel2State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel2Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLevel2Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel2State,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level2State: { value: input.level2State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level2State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel2InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountSystemProgram
  >);
}

export type ParsedInitLevel2Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level2State: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitLevel2InstructionData;
};

export function parseInitLevel2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLevel2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level2State: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLevel2InstructionDataDecoder().decode(instruction.data),
  };
}
