/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel1StatePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const VERIFY_AND_CLOSE_LEVEL1_DISCRIMINATOR = new Uint8Array([
  148, 124, 192, 9, 124, 227, 175, 140,
]);

export function getVerifyAndCloseLevel1DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_AND_CLOSE_LEVEL1_DISCRIMINATOR,
  );
}

export type VerifyAndCloseLevel1Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel1State extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel1State extends string
        ? WritableAccount<TAccountLevel1State>
        : TAccountLevel1State,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyAndCloseLevel1InstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VerifyAndCloseLevel1InstructionDataArgs = {};

export function getVerifyAndCloseLevel1InstructionDataEncoder(): FixedSizeEncoder<VerifyAndCloseLevel1InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VERIFY_AND_CLOSE_LEVEL1_DISCRIMINATOR,
    }),
  );
}

export function getVerifyAndCloseLevel1InstructionDataDecoder(): FixedSizeDecoder<VerifyAndCloseLevel1InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVerifyAndCloseLevel1InstructionDataCodec(): FixedSizeCodec<
  VerifyAndCloseLevel1InstructionDataArgs,
  VerifyAndCloseLevel1InstructionData
> {
  return combineCodec(
    getVerifyAndCloseLevel1InstructionDataEncoder(),
    getVerifyAndCloseLevel1InstructionDataDecoder(),
  );
}

export type VerifyAndCloseLevel1AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel1State extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level1State?: Address<TAccountLevel1State>;
};

export async function getVerifyAndCloseLevel1InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel1State extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel1AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyAndCloseLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level1State: { value: input.level1State ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level1State.value) {
    accounts.level1State.value = await findLevel1StatePda({
      user: expectAddress(accounts.user.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level1State),
    ],
    data: getVerifyAndCloseLevel1InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State
  >);
}

export type VerifyAndCloseLevel1Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel1State extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level1State: Address<TAccountLevel1State>;
};

export function getVerifyAndCloseLevel1Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel1State extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel1Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State
  >,
  config?: { programAddress?: TProgramAddress },
): VerifyAndCloseLevel1Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel1State
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level1State: { value: input.level1State ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level1State),
    ],
    data: getVerifyAndCloseLevel1InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel1Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel1State
  >);
}

export type ParsedVerifyAndCloseLevel1Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level1State: TAccountMetas[2];
  };
  data: VerifyAndCloseLevel1InstructionData;
};

export function parseVerifyAndCloseLevel1Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyAndCloseLevel1Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level1State: getNextAccount(),
    },
    data: getVerifyAndCloseLevel1InstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
