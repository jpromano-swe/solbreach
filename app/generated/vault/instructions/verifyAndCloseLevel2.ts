/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel2StatePda, findProfilePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const VERIFY_AND_CLOSE_LEVEL2_DISCRIMINATOR = new Uint8Array([
  91, 176, 147, 203, 107, 214, 51, 62,
]);

export function getVerifyAndCloseLevel2DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_AND_CLOSE_LEVEL2_DISCRIMINATOR,
  );
}

export type VerifyAndCloseLevel2Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel2State extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel2State extends string
        ? WritableAccount<TAccountLevel2State>
        : TAccountLevel2State,
      TAccountProfile extends string
        ? ReadonlyAccount<TAccountProfile>
        : TAccountProfile,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyAndCloseLevel2InstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VerifyAndCloseLevel2InstructionDataArgs = {};

export function getVerifyAndCloseLevel2InstructionDataEncoder(): FixedSizeEncoder<VerifyAndCloseLevel2InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VERIFY_AND_CLOSE_LEVEL2_DISCRIMINATOR,
    }),
  );
}

export function getVerifyAndCloseLevel2InstructionDataDecoder(): FixedSizeDecoder<VerifyAndCloseLevel2InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVerifyAndCloseLevel2InstructionDataCodec(): FixedSizeCodec<
  VerifyAndCloseLevel2InstructionDataArgs,
  VerifyAndCloseLevel2InstructionData
> {
  return combineCodec(
    getVerifyAndCloseLevel2InstructionDataEncoder(),
    getVerifyAndCloseLevel2InstructionDataDecoder(),
  );
}

export type VerifyAndCloseLevel2AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel2State extends string = string,
  TAccountProfile extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level2State?: Address<TAccountLevel2State>;
  profile?: Address<TAccountProfile>;
};

export async function getVerifyAndCloseLevel2InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel2State extends string,
  TAccountProfile extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel2AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountProfile
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyAndCloseLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountProfile
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level2State: { value: input.level2State ?? null, isWritable: true },
    profile: { value: input.profile ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level2State.value) {
    accounts.level2State.value = await findLevel2StatePda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.profile.value) {
    accounts.profile.value = await findProfilePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level2State),
      getAccountMeta(accounts.profile),
    ],
    data: getVerifyAndCloseLevel2InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountProfile
  >);
}

export type VerifyAndCloseLevel2Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel2State extends string = string,
  TAccountProfile extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level2State: Address<TAccountLevel2State>;
  profile: Address<TAccountProfile>;
};

export function getVerifyAndCloseLevel2Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel2State extends string,
  TAccountProfile extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: VerifyAndCloseLevel2Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountProfile
  >,
  config?: { programAddress?: TProgramAddress },
): VerifyAndCloseLevel2Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel2State,
  TAccountProfile
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level2State: { value: input.level2State ?? null, isWritable: true },
    profile: { value: input.profile ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level2State),
      getAccountMeta(accounts.profile),
    ],
    data: getVerifyAndCloseLevel2InstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyAndCloseLevel2Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel2State,
    TAccountProfile
  >);
}

export type ParsedVerifyAndCloseLevel2Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level2State: TAccountMetas[2];
    profile: TAccountMetas[3];
  };
  data: VerifyAndCloseLevel2InstructionData;
};

export function parseVerifyAndCloseLevel2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyAndCloseLevel2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level2State: getNextAccount(),
      profile: getNextAccount(),
    },
    data: getVerifyAndCloseLevel2InstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
