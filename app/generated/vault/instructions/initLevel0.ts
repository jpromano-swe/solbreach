/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findLevel0StatePda, findUserStatsPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INIT_LEVEL0_DISCRIMINATOR = new Uint8Array([
  251, 245, 62, 63, 138, 203, 254, 44,
]);

export function getInitLevel0DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_LEVEL0_DISCRIMINATOR);
}

export type InitLevel0Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserStats extends string | AccountMeta<string> = string,
  TAccountLevel0State extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserStats extends string
        ? WritableAccount<TAccountUserStats>
        : TAccountUserStats,
      TAccountLevel0State extends string
        ? WritableAccount<TAccountLevel0State>
        : TAccountLevel0State,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLevel0InstructionData = { discriminator: ReadonlyUint8Array };

export type InitLevel0InstructionDataArgs = {};

export function getInitLevel0InstructionDataEncoder(): FixedSizeEncoder<InitLevel0InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INIT_LEVEL0_DISCRIMINATOR }),
  );
}

export function getInitLevel0InstructionDataDecoder(): FixedSizeDecoder<InitLevel0InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitLevel0InstructionDataCodec(): FixedSizeCodec<
  InitLevel0InstructionDataArgs,
  InitLevel0InstructionData
> {
  return combineCodec(
    getInitLevel0InstructionDataEncoder(),
    getInitLevel0InstructionDataDecoder(),
  );
}

export type InitLevel0AsyncInput<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel0State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats?: Address<TAccountUserStats>;
  level0State?: Address<TAccountLevel0State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitLevel0InstructionAsync<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel0State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel0AsyncInput<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level0State: { value: input.level0State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.userStats.value) {
    accounts.userStats.value = await findUserStatsPda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.level0State.value) {
    accounts.level0State.value = await findLevel0StatePda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level0State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel0InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State,
    TAccountSystemProgram
  >);
}

export type InitLevel0Input<
  TAccountUser extends string = string,
  TAccountUserStats extends string = string,
  TAccountLevel0State extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userStats: Address<TAccountUserStats>;
  level0State: Address<TAccountLevel0State>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitLevel0Instruction<
  TAccountUser extends string,
  TAccountUserStats extends string,
  TAccountLevel0State extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitLevel0Input<
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLevel0Instruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserStats,
  TAccountLevel0State,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userStats: { value: input.userStats ?? null, isWritable: true },
    level0State: { value: input.level0State ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.userStats),
      getAccountMeta(accounts.level0State),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitLevel0InstructionDataEncoder().encode({}),
    programAddress,
  } as InitLevel0Instruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserStats,
    TAccountLevel0State,
    TAccountSystemProgram
  >);
}

export type ParsedInitLevel0Instruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userStats: TAccountMetas[1];
    level0State: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitLevel0InstructionData;
};

export function parseInitLevel0Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLevel0Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userStats: getNextAccount(),
      level0State: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLevel0InstructionDataDecoder().decode(instruction.data),
  };
}
