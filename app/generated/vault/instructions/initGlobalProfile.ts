/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findProfilePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INIT_GLOBAL_PROFILE_DISCRIMINATOR = new Uint8Array([
  58, 62, 122, 41, 39, 80, 228, 178,
]);

export function getInitGlobalProfileDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_GLOBAL_PROFILE_DISCRIMINATOR,
  );
}

export type InitGlobalProfileInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitGlobalProfileInstructionData = {
  discriminator: ReadonlyUint8Array;
  initialCommander: Address;
};

export type InitGlobalProfileInstructionDataArgs = {
  initialCommander: Address;
};

export function getInitGlobalProfileInstructionDataEncoder(): FixedSizeEncoder<InitGlobalProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["initialCommander", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_GLOBAL_PROFILE_DISCRIMINATOR }),
  );
}

export function getInitGlobalProfileInstructionDataDecoder(): FixedSizeDecoder<InitGlobalProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["initialCommander", getAddressDecoder()],
  ]);
}

export function getInitGlobalProfileInstructionDataCodec(): FixedSizeCodec<
  InitGlobalProfileInstructionDataArgs,
  InitGlobalProfileInstructionData
> {
  return combineCodec(
    getInitGlobalProfileInstructionDataEncoder(),
    getInitGlobalProfileInstructionDataDecoder(),
  );
}

export type InitGlobalProfileAsyncInput<
  TAccountAdmin extends string = string,
  TAccountProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  profile?: Address<TAccountProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  initialCommander: InitGlobalProfileInstructionDataArgs["initialCommander"];
};

export async function getInitGlobalProfileInstructionAsync<
  TAccountAdmin extends string,
  TAccountProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitGlobalProfileAsyncInput<
    TAccountAdmin,
    TAccountProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitGlobalProfileInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProfile,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    profile: { value: input.profile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.profile.value) {
    accounts.profile.value = await findProfilePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.profile),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitGlobalProfileInstructionDataEncoder().encode(
      args as InitGlobalProfileInstructionDataArgs,
    ),
    programAddress,
  } as InitGlobalProfileInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProfile,
    TAccountSystemProgram
  >);
}

export type InitGlobalProfileInput<
  TAccountAdmin extends string = string,
  TAccountProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  profile: Address<TAccountProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  initialCommander: InitGlobalProfileInstructionDataArgs["initialCommander"];
};

export function getInitGlobalProfileInstruction<
  TAccountAdmin extends string,
  TAccountProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitGlobalProfileInput<
    TAccountAdmin,
    TAccountProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitGlobalProfileInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountProfile,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    profile: { value: input.profile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.profile),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitGlobalProfileInstructionDataEncoder().encode(
      args as InitGlobalProfileInstructionDataArgs,
    ),
    programAddress,
  } as InitGlobalProfileInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountProfile,
    TAccountSystemProgram
  >);
}

export type ParsedInitGlobalProfileInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    profile: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitGlobalProfileInstructionData;
};

export function parseInitGlobalProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitGlobalProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      profile: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitGlobalProfileInstructionDataDecoder().decode(instruction.data),
  };
}
